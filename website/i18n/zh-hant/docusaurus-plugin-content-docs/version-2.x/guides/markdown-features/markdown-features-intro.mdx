---
id: introduction
description: Docusaurus uses MDX. Find out more about Docusaurus-specific features when writing Markdown.
slug: /markdown-features
---

# Markdown 功能

import BrowserWindow from '@site/src/components/BrowserWindow';

文件是產品與使用者互動的重要介面。一套撰寫良好且組織完善的文檔能幫助使用者快速理解您的產品。我們的共同目標是協助使用者以最快速度找到並理解所需資訊。

Docusaurus 2 採用現代化工具，讓您輕鬆編寫互動式文檔。您可以嵌入 React 元件，或建立即時編碼區塊讓使用者現場操作程式碼。這是吸引潛在使用者最有效的方式，讓您的靈感時刻透過程式碼直接呈現給觀眾。

:::info

本節假設您正在使用官方 Docusaurus 內容插件。

:::

## 標準功能 {#standard-features}

Markdown 是一種讓您能以易讀語法編寫格式化內容的標記語言。

我們使用 [MDX](https://mdxjs.com/) 作為解析引擎，它不僅能解析[標準 Markdown 語法](https://daringfireball.net/projects/markdown/syntax)，還能在文件中渲染 React 元件。

```md
### My Doc Section

Hello world message with some **bold** text, some _italic_ text, and a [link](/)

![img alt](/img/docusaurus.png)
```

```mdx-code-block
<BrowserWindow>

<h3>My Doc Section</h3>

Hello world message with some **bold** text, some _italic_ text and a [link](/)

![img alt](/img/docusaurus.png)

</BrowserWindow>
```

<details>

<summary>Markdown is declarative</summary>

Some may assume a 1-1 correlation between Markdown and HTML, e.g., `![Preview](/img/docusaurus.png)` will always become `<img src="/img/docusaurus.png" alt="Preview" />`, as-is. However, _that is not the case_.

The Markdown syntax `![message](url)` only declaratively tells Docusaurus that an image needs to be inserted here, but we may do other things like transforming a [file path to URL path](./markdown-features-assets.mdx#images), so the generated markup may differ from the output of other Markdown renderers, or a naïve hand-transcription to the equivalent JSX/HTML code.

In general, you should only assume the _semantics_ of the markup (` ``` ` fences become [code blocks](./markdown-features-code-blocks.mdx); `>` becomes [quotes](#quotes), etc.), but not the actual compiled output.

</details>

## Front matter {#front-matter}

Front matter 用於為 Markdown 文件添加元數據。所有內容插件都有各自的 front matter 結構，並利用這些數據來豐富從內容或其他配置推斷出的默認元數據。

Front matter 位於文件最頂部，由三個破折號 `---` 包圍。內容以 [YAML](https://yaml.org/spec/1.2.2/) 格式解析。

```md
---
title: My Doc Title
more_data:
  - Can be provided
  - as: objects
    or: arrays
---
```

## 引用 {#quotes}

Markdown 引用區塊具有精美樣式：

```md
> Easy to maintain open source documentation websites.
>
> — Docusaurus
```

<BrowserWindow>

> Easy to maintain open source documentation websites.
>
> — Docusaurus

</BrowserWindow>

## 詳情區塊 {#details}

Markdown 可嵌入 HTML 元素，且 [`details`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/details) HTML 元素具有精美樣式：

```md
### Details element example

<details>
  <summary>Toggle me!</summary>
  <div>
    <div>This is the detailed content</div>
    <br/>
    <details>
      <summary>
        Nested toggle! Some surprise inside...
      </summary>
      <div>😲😲😲😲😲</div>
    </details>
  </div>
</details>
```

```mdx-code-block
<BrowserWindow>

<h3>Details element example</h3>

<details>
  <summary>Toggle me!</summary>
  <div>
    <div>This is the detailed content</div>
    <br/>
    <details>
      <summary>
        Nested toggle! Some surprise inside...
      </summary>
      <div>😲😲😲😲😲</div>
    </details>
  </div>
</details>

</BrowserWindow>
```

:::note

實際上這些並非真正的 HTML 元素，而是 React JSX 元素，我們將在後續章節說明！

:::