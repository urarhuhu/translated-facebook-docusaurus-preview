---
sidebar_position: 1
slug: /api/misc/@docusaurus/eslint-plugin
---

# 📦 eslint-plugin

[ESLint](https://eslint.org/) 是一個靜態分析程式碼的工具，能透過編輯器提示和命令列回報問題或建議最佳實踐。Docusaurus 提供了一個 ESLint 插件來強制執行 Docusaurus 的最佳實踐。

## 安裝

```bash npm2yarn
npm install --save-dev @docusaurus/eslint-plugin
```

## 使用方式

### 推薦配置

將 `plugin:@docusaurus/recommended` 加入你的 `.eslintrc` 設定檔的 `extends` 區塊：

```json title=".eslintrc"
{
  "extends": ["plugin:@docusaurus/recommended"]
}
```

這會啟用 `@docusaurus` eslint 插件並使用 `recommended` 配置。請參閱下方的[支援規則](#supported-rules)以了解此配置會啟用的規則清單。

### 手動配置

若需要更細緻的控制，你也可以手動啟用插件並直接配置你想使用的規則：

```json title=".eslintrc"
{
  "plugins": ["@docusaurus"],
  "rules": {
    "@docusaurus/string-literal-i18n-messages": "error",
    "@docusaurus/no-untranslated-text": "warn"
  }
}
```

## 支援的配置

- 推薦配置：適用於大多數 Docusaurus 網站的推薦規則集，應從此配置延伸。
- 全部規則：啟用**所有**規則。這會在次要版本之間變更，因此若想避免意外的破壞性變更，不應使用此配置。

## 支援的規則

| Name | Description |  |
| --- | --- | --- |
| [`@docusaurus/no-untranslated-text`](./no-untranslated-text.mdx) | Enforce text labels in JSX to be wrapped by translate calls |  |
| [`@docusaurus/string-literal-i18n-messages`](./string-literal-i18n-messages.mdx) | Enforce translate APIs to be called on plain text labels | ✅ |
| [`@docusaurus/no-html-links`](./no-html-links.mdx) | Ensures @docusaurus/Link is used instead of `<a>` tags | ✅ |
| [`@docusaurus/prefer-docusaurus-heading`](./prefer-docusaurus-heading.mdx) | Ensures @theme/Heading is used instead of `<hn>` tags for headings | ✅ |

✅ = 推薦

## 範例配置

以下是一個範例配置：

```js title=".eslintrc.js"
module.exports = {
  extends: ['plugin:@docusaurus/recommended'],
  rules: {
    '@docusaurus/no-untranslated-text': [
      'warn',
      {ignoredStrings: ['·', '—', '×']},
    ],
  },
};
```